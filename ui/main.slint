import { Button, VerticalBox, HorizontalBox, LineEdit, ScrollView, ListView } from "std-widgets.slint";

struct PartnerData {
    id: string,
    nazev: string,
    slozka: string,
    aktualizovano: string,
    ma_slozku: bool, // Pro vizuální barvu puntíku
}

// 1. KOMPONENTA TLAČÍTKA
component MenuButton inherits Rectangle {
    in-out property<string> text;
    in-out property<bool> active;
    callback clicked;
    height: 45px;
    background: active ? #2CC985 : touch.has-hover ? #3d3d3d : #2e2e2e;
    border-radius: 6px;
    border-width: 1px;
    border-color: active ? #2CC985 : #3f3f3f;
    touch := TouchArea { clicked => { root.clicked(); } }
    HorizontalLayout {
        padding-left: 20px;
        Text {
            text: root.text; color: root.active ? #1A1A1A : white;
            vertical-alignment: center; font-size: 14px; font-weight: root.active ? 800 : 500;
        }
    }
}

// 2. NOVÉ SAMOSTATNÉ OKNO PROGRESS BARU
export component ProgressWindow inherits Window {
    in-out property <float> progress: 0.0;
    in-out property <string> status: "Připravuji...";
    
    title: "Import Dat";
    width: 400px;
    height: 180px;
    background: #212121;
    always-on-top: true; // Zůstane vždy nad hlavním oknem
    no-frame: false;     // Má rám (křížek atd.), pro modernější vzhled lze dát true (ale pak nejde posouvat)

    VerticalLayout {
        padding: 30px; spacing: 20px; alignment: center;
        
        Text { 
            text: root.status; 
            color: white; font-size: 18px; font-weight: 700; horizontal-alignment: center; 
        }

        // Progress Bar
        Rectangle {
            width: 300px; height: 12px; background: #333; border-radius: 6px;
            Rectangle {
                width: root.progress * 300px; height: 12px; 
                background: #2CC985; border-radius: 6px; x: 0;
                animate width { duration: 100ms; }
            }
        }

        Text { 
            text: round(root.progress * 100) + "%"; 
            color: #888; horizontal-alignment: center; 
        }
    }
}

// 3. HLAVNÍ OKNO APLIKACE (Bez progress logiky)
export component AppWindow inherits Window {
    in-out property <string> verze_aplikace: "0.3.1";
    in-out property <string> cesta_archiv: "";
    in-out property <string> cesta_vyroba: "";
    in-out property <string> posledni_sync_cas: "Nikdy";
    in-out property <bool> databaze_ok: false;
    in-out property <string> stav_text: "DATABÁZE NENÍ NAČTENA";
    in-out property <[PartnerData]> model_partneru: [];

    property <int> vybrana_stranka: 0; 
    property <int> settings_tab: 0;

    callback vybrat_archiv();
    callback vybrat_vyrobu();
    callback ulozit_nastaveni();
    callback spustit_synchronizaci(); // Rust toto zavolá a otevře druhé okno

    title: "MRB Obchodník - v" + verze_aplikace;
    min-width: 900px; min-height: 650px;
    background: #1A1A1A;

    HorizontalLayout {
        padding: 0; spacing: 0;

        // LEVÝ PANEL
        Rectangle {
            width: 280px; background: #212121;
            VerticalLayout {
                padding: 25px; spacing: 10px;
                Text { text: "MRB Obchodník"; color: #2CC985; font-size: 20px; font-weight: 900; horizontal-alignment: center; }
                Rectangle { height: 20px; }
                MenuButton { text: "🏠  Domovská obrazovka"; active: vybrana_stranka == 0; clicked => { vybrana_stranka = 0; } }
                MenuButton { text: "📂  Nový import"; active: vybrana_stranka == 1; clicked => { vybrana_stranka = 1; } }
                MenuButton { text: "⚙  Nastavení systému"; active: vybrana_stranka == 2; clicked => { vybrana_stranka = 2; } }
                Rectangle { vertical-stretch: 1; }
                Text { text: "v" + root.verze_aplikace; color: #555; font-size: 11px; horizontal-alignment: center; }
            }
        }

        // PRAVÝ OBSAH
        Rectangle {
            background: #1A1A1A;
            
            if (vybrana_stranka == 0) : VerticalBox {
                alignment: center; spacing: 20px;
                Text { text: "🏠"; font-size: 60px; horizontal-alignment: center; }
                Text { text: "Vítejte v systému"; font-size: 30px; font-weight: 800; color: white; horizontal-alignment: center; }
            }

            if (vybrana_stranka == 1) : VerticalBox {
                alignment: center;
                Text { text: "🚧"; font-size: 60px; horizontal-alignment: center; }
                Text { text: "Sekce Import"; font-size: 24px; color: white; horizontal-alignment: center; }
            }

            if (vybrana_stranka == 2) : VerticalLayout {
                padding: 40px; spacing: 20px;
                HorizontalBox {
                    height: 45px; spacing: 10px; padding: 0;
                    Button { text: "Cesty"; primary: settings_tab == 0; clicked => { settings_tab = 0; } }
                    Button { text: "Synchronizace"; primary: settings_tab == 1; clicked => { settings_tab = 1; } }
                    Button { text: "Správce dat"; primary: settings_tab == 2; clicked => { settings_tab = 2; } }
                }

                Rectangle {
                    vertical-stretch: 1;
                    
                    if (settings_tab == 0) : VerticalLayout {
                        vertical-stretch: 1; alignment: start; spacing: 20px;
                        VerticalBox {
                            spacing: 5px; padding: 0;
                            Text { text: "CESTA K ARCHIVU"; color: #888; font-size: 11px; font-weight: 700; }
                            HorizontalLayout {
                                spacing: 10px; height: 40px;
                                LineEdit { text: cesta_archiv; placeholder-text: "Vyberte složku..."; height: 40px; }
                                Button { text: "Procházet"; width: 100px; clicked => { root.vybrat_archiv(); } }
                            }
                        }
                        VerticalBox {
                            spacing: 5px; padding: 0;
                            Text { text: "CESTA PRO VÝROBU"; color: #888; font-size: 11px; font-weight: 700; }
                            HorizontalLayout {
                                spacing: 10px; height: 40px;
                                LineEdit { text: cesta_vyroba; placeholder-text: "Vyberte složku..."; height: 40px; }
                                Button { text: "Procházet"; width: 100px; clicked => { root.vybrat_vyrobu(); } }
                            }
                        }
                        Rectangle { vertical-stretch: 1; }
                        Button { text: "ULOŽIT KONFIGURACI"; height: 50px; primary: true; clicked => { root.ulozit_nastaveni(); } }
                    }

                    if (settings_tab == 1) : VerticalLayout {
                        vertical-stretch: 1; spacing: 25px;
                        Text { text: "Synchronizace partnerů"; font-size: 22px; font-weight: 700; color: white; horizontal-alignment: center; }
                        Rectangle {
                            border-color: databaze_ok ? #2CC985 : #e74c3c; border-width: 2px; border-radius: 10px; height: 100px;
                            Text { text: root.stav_text; color: databaze_ok ? #2CC985 : #e74c3c; font-size: 20px; font-weight: 800; horizontal-alignment: center; vertical-alignment: center; }
                        }
                        VerticalLayout {
                            spacing: 5px;
                            Text { text: "Poslední aktualizace proběhla:"; color: #888; font-size: 14px; horizontal-alignment: center; }
                            Text { text: root.posledni_sync_cas; color: white; font-size: 20px; font-weight: 600; horizontal-alignment: center; }
                        }
                        Rectangle { vertical-stretch: 1; }
                        Button { text: "NAHRÁT EXCEL A SPUSTIT"; height: 50px; primary: true; clicked => { root.spustit_synchronizaci(); } }
                    }

                    if (settings_tab == 2) : VerticalLayout {
                        vertical-stretch: 1;
                        spacing: 15px;
                        padding-top: 10px;

                        // 1. HORNÍ INFO PANELY (Statistiky)
                        HorizontalLayout {
                            height: 60px;
                            spacing: 15px;
        
                            // Karta: Celkem
                            Rectangle {
                                background: #252525; border-radius: 8px;
                                HorizontalLayout {
                                    padding: 15px; spacing: 15px;
                                    Text { text: "👥"; font-size: 20px; vertical-alignment: center; }
                                    VerticalLayout {
                                        alignment: center;
                                        Text { text: "Celkem partnerů"; color: #888; font-size: 11px; }
                                        Text { text: "254"; color: white; font-size: 16px; font-weight: 700; }
                                    }
                                }
                            }
        
                            // Karta: Chybějící složky
                            Rectangle {
                                background: #252525; border-radius: 8px;
                                HorizontalLayout {
                                    padding: 15px; spacing: 15px;
                                    Text { text: "📁"; font-size: 20px; vertical-alignment: center; }
                                    VerticalLayout {
                                        alignment: center;
                                        Text { text: "Chybí složka"; color: #888; font-size: 11px; }
                                        Text { text: "12"; color: #e74c3c; font-size: 16px; font-weight: 700; }
                                    }
                                }
                            }
        
                            // Vyhledávací lišta (zabere zbytek místa)
                            Rectangle {
                                background: #2e2e2e; border-radius: 8px;
                                horizontal-stretch: 1;
                                HorizontalLayout {
                                    padding-left: 15px; padding-right: 15px;
                                    Text { text: "🔍"; vertical-alignment: center; }
                                    LineEdit { 
                                        placeholder-text: "Hledat podle názvu nebo ID partnera..."; 
                                        font-size: 14px;
                                    }
                                }
                            }
                        }

                        // 2. HLAVIČKA TABULKY (Pevná)
                        Rectangle {
                        height: 40px; background: #2a2a2a; border-radius: 6px;
                         HorizontalLayout {
                            padding-left: 20px; padding-right: 20px;
                            Text { text: "STAV"; width: 80px; color: #666; font-size: 11px; font-weight: 800; vertical-alignment: center; }
                            Text { text: "ID"; width: 100px; color: #666; font-size: 11px; font-weight: 800; vertical-alignment: center; }
                            Text { text: "NÁZEV PARTNERA"; width: 300px; color: #666; font-size: 11px; font-weight: 800; vertical-alignment: center; }
                            Text { text: "PŘIŘAZENÁ SLOŽKA"; color: #666; font-size: 11px; font-weight: 800; vertical-alignment: center; }
                            Text { text: "AKCE"; width: 100px; color: #666; font-size: 11px; font-weight: 800; vertical-alignment: center; horizontal-alignment: center; }
                            }
                        }

                        // 3. SEZNAM DAT (Optimalizováno pro 14 000+ položek)
                        ListView {
                            for partner in root.model_partneru : Rectangle {
                                height: 50px; // Důležité: ListView musí znát výšku řádku
                                background: row-touch.has-hover ? #252525 : #1e1e1e; // Hover efekt
                                border-radius: 6px;
        
                                row-touch := TouchArea {}

                                HorizontalLayout {
                                    padding-left: 20px; padding-right: 20px;
            
                                    // 1. Indikátor stavu (Puntík)
                                    Rectangle { 
                                        width: 60px; 
                                        VerticalLayout {
                                            alignment: center;
                                            Rectangle { 
                                                width: 12px; height: 12px; border-radius: 6px; 
                                                background: partner.ma-slozku ? #2CC985 : #e74c3c; 
                                            }
                                        }
                                    }

                                    // 2. ID
                                    Text { 
                                        text: partner.id; width: 100px; 
                                        color: #aaa; vertical-alignment: center; 
                                    }

                                    // 3. Název
                                    Text { 
                                        text: partner.nazev; width: 300px; 
                                        color: white; font-weight: 600; vertical-alignment: center; 
                                        overflow: elide;
                                    }
            
                                    // 4. Cesta (zkrácená)
                                    Text { 
                                        text: partner.slozka == "" ? "--- nepřiřazeno ---" : partner.slozka; 
                                        color: partner.slozka == "" ? #444 : #888; 
                                        font-size: 13px; vertical-alignment: center; 
                                        overflow: elide; // Přidá "..." pokud je text dlouhý
                                        horizontal-stretch: 1; // Zabere zbytek místa
                                    }

                                    // 5. Tlačítko akce
                                    Rectangle {
                                        width: 100px;
                                        VerticalLayout {
                                            alignment: center;
                                            Button { text: "Detail"; width: 80px; height: 30px; }
                                        }
                                    }
                                }
                             }
                        }
                    }
                }
            }
        }
    }
}

